# Developer Tools v1.0.9

静态网页开发工具箱，支持中文 / English，支持 PC 和移动端，可部署到 GitHub Pages。

## 本版本更新

- 替换 PC 浏览器图标和 iPhone Safari 添加到主屏幕图标。
- 图标改为更暗、更低调的深蓝 / 蓝绿色渐变风格，整体更简约舒服。
- 版本号更新为 v1.0.9。

## 当前工具

1. String Concatenation
   - 支持输入格式 + 输出格式互转。
   - 支持换行、逗号、单引号、双引号、无分隔格式。

2. Excel ⇔ JSON
   - Excel 转 JSON。
   - JSON 转 Excel。
   - 支持多 Sheet。

## 部署

上传整个 `dev-tools-web` 目录到 GitHub Pages 即可。

当前使用 CDN：

- jQuery
- xlsx.full.min.js

## 历史更新

### v1.0.9

- 重新设计 PC favicon、移动端 apple-touch-icon、manifest 图标。
- 图标改为低亮度深蓝 / 蓝绿色渐变，更低调简约。

### v1.0.8

- 重新设计下拉框、按钮、Tab、文件选择等控件视觉。
- 检查并修正中英文语言包，中文模式下菜单和页面文案不再混用英文。

### v1.0.7

- PC 端改为固定工作台布局，默认隐藏浏览器滚动条。
- String Concatenation 输入 / 输出区调整为左右双编辑器布局。
- Excel ⇔ JSON 工具区调整为顶部参数区、操作区、主编辑区。
